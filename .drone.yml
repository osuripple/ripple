build:
  image: php
  commands:
    - find . -type f -iname '*.php' -printf 'php -l %p || exit $?\n' | bash
notify:
  slack:
    username: Schiavo
    channel: ripple
    webhook_url: $$SLACKURL
    when:
      success: true
      failure: true
      change: true
